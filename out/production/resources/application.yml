#서버 포트 설정
server:
  port: 8080

#database 연동 설정:
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

jpa:
 database-platform:
   open-in-view:false
  show-sql:true
 hibernate:
   ddl-auto: update